# Distributing Calendar (native macOS) — alpha runbook

A native SwiftUI app (`native/CalendarApp`) over the local `CalendarKit` package. Syncs each
user's data to **their own iCloud** via CloudKit (`CKSyncEngine`, private database) — no server.
Distributed as a **Developer-ID-signed, notarized `.dmg`** for direct download.

- **Size:** ~9 MB app → ~5–7 MB DMG. No bundled runtime/database (unlike the old `desktop/` build).
- **Requires:** macOS 26 (Tahoe), Apple Silicon. (Deployment target is pinned to 26.0.)

---

## One-time setup (Apple Developer account)

You currently have only an **Apple Development** certificate. Distribution needs three things:

1. **Developer ID Application certificate**
   Xcode → Settings → Accounts → your team → *Manage Certificates* → **+** → *Developer ID Application*.
   Verify:
   ```
   security find-identity -v -p codesigning   # look for "Developer ID Application: … (X84ZG75WAM)"
   ```

2. **Notary credentials** (stored once in the keychain)
   Create an app-specific password at <https://account.apple.com> → Sign-In & Security → App-Specific Passwords, then:
   ```
   xcrun notarytool store-credentials libirabu-notary \
     --apple-id "ziyang@cs.jhu.edu" --team-id X84ZG75WAM --password <app-specific-password>
   ```

3. **Deploy the CloudKit schema to Production** ⚠️ *easy to miss*
   A Developer-ID build talks to the **production** CloudKit environment, but the schema currently
   only exists in **development**. Open <https://icloud.developer.apple.com> → container
   `iCloud.dev.libirabu.calendar` → **Deploy Schema Changes** (Development → Production). Without
   this, testers' records are rejected and nothing syncs.

---

## Build a release

```
native/CalendarApp/scripts/package.sh                 # signed + notarized → build/Calendar.dmg
NOTARIZE=0 native/CalendarApp/scripts/package.sh       # local smoke test, skip Apple upload
```

The script: regenerates the project (XcodeGen) → `xcodebuild archive` → export with
`scripts/ExportOptions.plist` (Developer ID) → notarize + staple → DMG.

Verify the result opens cleanly on a clean Mac:
```
spctl -a -vvv -t install build/export/Calendar.app     # → source=Notarized Developer ID
```

---

## Push notifications (realtime sync) — optional for alpha

The shipped build uses the **development** APNs entitlement (`CalendarApp.entitlements`), so
CloudKit change-**push** may not deliver on testers' machines — sync still happens on app launch
and periodically. That's usually fine for an alpha.

To enable realtime push: enable **Push Notifications** for the App ID in the developer portal,
then point the Release config at the production variant:

```yaml
# native/CalendarApp/project.yml → CalendarApp target
    settings:
      base: { … CODE_SIGN_ENTITLEMENTS: CalendarApp.entitlements … }   # Debug keeps development APNs
      configs:
        Release:
          CODE_SIGN_ENTITLEMENTS: CalendarApp-Release.entitlements       # production APNs
```

(`CalendarApp-Release.entitlements` already exists with `aps-environment = production`.)

---

## The app icon

Minimalist calendar mark, generated by `scripts/make-icon.swift` into
`Assets.xcassets/AppIcon.appiconset`. To change the design, edit the generator and rerun:
```
swift native/CalendarApp/scripts/make-icon.swift /tmp/icon_1024.png
# then regenerate the sized PNGs into the appiconset (see the sips loop used to create them)
```

---

## Versioning

Bump per alpha drop in `project.yml`: `MARKETING_VERSION` (e.g. 0.1 → 0.2) and
`CURRENT_PROJECT_VERSION` (build number, must increase for each notarized upload).

---

## Recipient instructions (what to tell testers)

> Requires **macOS 26 (Tahoe)** on **Apple Silicon**, and you must be **signed into iCloud**
> (System Settings → your name) for your calendar to sync across your devices.
>
> 1. Open `Calendar.dmg`, drag **Calendar** to **Applications**.
> 2. Launch it. (Notarized, so it opens without the "unidentified developer" block.)
> 3. Your events sync to your **own** iCloud — nothing goes to any server.

---

## Known alpha limitations

- **Search** and **Assistant** toolbar buttons are placeholders (no action wired yet).
- macOS 26-only (Liquid Glass toolbar APIs); no fallback for older macOS.
- No in-app "not signed into iCloud" messaging — sync silently no-ops if there's no account.
- No crash reporting / feedback channel.
- CloudKit schema is additive-only once in production — plan migrations before the next drop.
